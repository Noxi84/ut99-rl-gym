# Journal 2026-06-13 — CTF-Face vallen: trainer-resume + edge-exposure PBRS (structureel)

## Context
User-opdracht (vervolg op 06-10/06-11 nacht): bots vallen NOG steeds van CTF-Face en halen de
vlag niet. Servers draaiden ("nu aan"). Eisen: boven-pro op Face, autonoom doorgaan, efficiënt met
mens-tijd/servers, **GEEN BC-baseline-reset** (backup/champion-restore bij corruptie), **structurele
oplossing** (nieuwe map / from-scratch moet zelf leren zónder handmatige config-tweaks), code+reward
changes vrij (liefst generiek, map-specifiek mag).

## Diagnose (investigatie-eerst, geen aannames)
**Bevinding 1 — DE TRAINER STOND 2 DAGEN UIT.** `rl_pawn_sac` trainer.log eindigt SCHOON op
2026-06-11 19:36 (step 181200, ret 1.73, geen crash — sessie-einde). User herstartte vandaag 10:32
alleen de BOTS, niet de SAC-trainer. 4090: enkel `bots`-tmux, geen python-proces, GPU 0%, VRAM 28MB.
→ Model 2 dagen BEVROREN op step 181000 (best_exported 3.09), draaide enkel inference. Dat alléén
verklaart "valt nog steeds": het clean-nav-graph-geodesic-veld-experiment (Round 19-20, 06-11) kreeg
NOOIT zijn trainingstijd (trainer stierf 2u na deploy). checkpoint INTACT (schone shutdown), baseline
null → resume = no-reset-pad.

**Bevinding 2 — val-mechanisme bevestigd op verse traces (vandaag).** Raw position-trace (4070):
BETA-bots vallen door ZIJWAARTSE afdrijving van de Y-zijranden TIJDENS +X-oprukken. Voorbeeld
[BETA]Falcon: spawn (-1592,-1308,-2003) → val-onset (-534,-2400,-4744): +1058 in X (richting midden)
ÉN -1092 in Y (voorbij de -1637 zijrail) → plons. = exact user's klacht; bevestigt journal Round 15/19.
selfDamage -50/-100 per window (val-clamp vuurt) = onafhankelijke bevestiging dat het NU nog gebeurt.

**Bevinding 3 — STRUCTURELE blinde vlek in de val-perceptie/straf (waarom Fix F faalde).**
- Movement is ONTKOPPELD van aim: UC `RLUdpCommandReceiver` zet `RLTargetYaw` (kijken/mikken) én
  `RLMoveYaw` (bewegen) als APARTE velden. Mikken op een off-axis enemy stuurt de bot dus NIET
  mechanisch van de brug — de policy kiest een eigen moveYaw-heading die de void in drijft.
- `floor_drop_penalty` (Fix F, 06-11) leest de floor-delta van de movement-primitive, MAAR de
  decoder snapt moveYaw naar 1 van 8 AIM-relatieve sectoren en leest enkel díe sector. Een
  'mostly-forward, slightly-toward-rail'-heading snapt naar FORWARD → leest `fwdFloorDelta` (brug ligt
  vooruit = veilig) → de laterale drift naar de zijrail is ONZICHTBAAR voor de straf. F was dus niet
  fout-getuned maar STRUCTUREEL blind (eenzijdig + single-sector-snap) → faalde + freeze.
- Floor-perceptie: `FloorProbeDist=160`, `FloorMaxDrop=1600`, 8 sectoren relatief aan RLTargetYaw.

## Actie 1 — Trainer RESUMED (grootste directe winst)
- Backup `rl_pawn_sac.pt` + `rl_pawn_sac_best.pt` → `models/backup_2026-06-13_pre_resume/`.
- `train-sac.sh rl_pawn`. Bootstrap-log: `Loaded SAC metadata: step=181000 best_exported=3.09`,
  `Warmup already completed`, `placeholder-null-baseline (1/1/1)` → rollback onderdrukt,
  `Entering main joint SAC loop`. = SCHONE resume op 181000 (geen revert naar 946000, geen warmup-reset;
  per journal-precedent = clean). Experience (868+ npz queued vandaag) stroomt in.

## Actie 2 — Edge-exposure PBRS reward (`void_avoidance`) — STRUCTUREEL, generiek
Nieuwe term in `movement_action` (movement-head, ACTION-categorie). De CORRECTE versie van wat Fix F
structureel verkeerd deed:
- `exposure(s)` = fractie van ALLE 8 floor-delta-sectoren met een val-grade drop (> `floor_drop_danger_
  threshold_uu`=560, lineair tot `void_avoidance_full_drop_uu`=1600). Richting-AGNOSTISCH → vangt de
  laterale drift die de 8-sector-snap mist.
- Potentiaal `Φ(s) = -scale·exposure(s)`; reward = PBRS-delta `F = Φ(curr) - Φ(prev)` (γ=1, conform
  quasi-PBRS objective_progress). Naderen van een rand (exposure↑) straft VÓÓR de val (anticipatief,
  ~160uu lookahead); terugkeren naar veilige vloer (exposure↓) BELOONT → symmetrisch (PBRS,
  policy-invariant) ⇒ GEEN permanente freeze zoals de eenzijdige Fix F.
- **Map-onafhankelijk/structureel:** op void-vrije maps (AndAction enclosed arena) is exposure overal
  0 ⇒ F=0 ⇒ NUL effect op bestaande getrainde maps/wapens; elke nieuwe void/lava/klif-map krijgt
  edge-vermijding GRATIS zonder config-tweak (= de "nieuwe map werkt vanzelf"-eis).
- **Reward-based ⇒ blijft plakken** (geen BC-forgetting; user-kritiek 06-11 geadresseerd).
- Respawn-guard: skip bij |Δz|>`z_jump_threshold`=500 (respawn uit void) of health-stijging
  (combat-death-respawn) — anders zou Φ over de dood→respawn-discontinuïteit telescoperen en
  dood+respawn netto BELONEN. clamp ±2.0 tegen critic-destabilisatie. Reversibel (scale 0 = uit).
- Params (default-blok, deep-merge vult alle rolegroups): `weights.void_avoidance_scale=5.0`,
  `void_avoidance_full_drop_uu=1600`, `void_avoidance_clamp_per_tick=2.0`,
  `void_avoidance_z_jump_threshold_uu=500`. Hergebruikt `floor_drop_danger_threshold_uu`=560 als drempel.
- Files: `RewardSignal.VOID_AVOIDANCE` (achteraan, ordinal-stabiel), `MovementActionParams/Module/
  Reward`, `RewardComputer` (.set + fallback-arity 7→8). Build groen. jq-valid. Catalog-parse-test
  (`JsonRewardCatalog.from("rl_pawn","Attack")`, rolegroup mét movement_action-override) RESOLVED
  void_avoidance via deep-merge → geen fleet-crash-risico (Round 5b-les).

## Deploy (TRAINER-VEILIG, journal-methode)
`sync-code.sh` naar alle 5 play-machines (jar + rewards.json + maps + geodesic veld; excl. baseline.json)
→ `start-bots.sh --all` (enkel `bots`-tmux per host; `sac_rl_pawn` op 4090 ONGEMOEID). GEEN
clean-experience (PBRS-term is klein/additief; resumed buffer blijft geldig). GEEN full deploy.sh
(zou trainer herstarten = corruptie-risico).

## Te monitoren (≥1.5u stabiel, no-thrash)
- **void-fractie / selfDamage MOET dalen** (her-meet position-traces Z-verdeling + selfDamage-term).
- **objectiveProgress → minder negatief/positief** (minder val→respawn-ver-resets).
- **X-spread BEHOUDEN** (geen freeze door void_avoidance; PBRS hoort dit te voorkomen — bij krimp scale→3).
- voidAvoidance breakdown-veld > 0 firing (anders niet actief); damageDealt/frags; eerste flag-caps.
- Trainer continu (etimes, step↑ vanaf 181k, 0 corruptie/NaN). Re-anchor baseline pas zodra KPI>0.
- Bij te zwak (vallen blijven): scale→7, of densify geodesic-veld (lateral swaths, journal Cycle 20-plan).

## Round 2 — User-observatie: aim-farm-stall + combat-investigatie
User (live): bots die net over midfield geraken schieten niet effectief, houden viewrotation op de
enemy, strafen heen-en-weer, en blijven daar bizar genoeg wél op de map.

**Data-bevindingen (corrigeren de oppervlakte-indruk):**
- Bots VUREN veel: trainer-probe `fire_rate=0.83`, `altFire_rate=0.96`, aim `cc2=0.94`; frags
  gebeuren (game-log). Combat is NIET dood.
- `damageDealt=0.00` is vrijwel zeker een MEET-artefact: `DamageDeltaReward` crediteert via
  `curr.enemies[i].lastDamageInstigatorSlot==ownSlot`, en die egocentrische enemies-array /
  slot-attributie is vaak leeg in de reward-context ([[project_reward_component_registration]]:
  "state.enemies vaak leeg → player1"). Veld leest 0 terwijl kills via een andere teller wél tellen.
  → bots zijn NIET incapabel; damageDealt/EFC-damage/headshot-rewards staan stil door de meting.
- **viewAlignment-farm is dominant**: `enemy_alignment_bonus=2.5` (~+497/window puur voor yaw-op-enemy,
  GEEN vuur/raak nodig) vs `objective_alignment_bonus=0.0` (NUL voor naar-objective-kijken). De
  enemy-aim-tak draait alleen bij enemy-attention; movement is ontkoppeld van view, dus de farm
  vormt WAAR de bot kijkt, niet direct of hij oprukt — maar de policy correleert "enemy aanwezig →
  jockey/strafe" i.p.v. doorduwen, en doorduwen = val-gevoelig terrein oversteken → stilstaan-bij-enemy
  is én hoogste-reward én laagste-val-risico. De twee user-klachten (vallen + niet-doorduwen) = één munt.
- **Enemy-base-reach (positie-traces, stall-meetlat):** ALPHA 35.9% bij enemy-base (X<1000),
  BETA 24.0% (X>5000). Bots zijn NIET hard-gestald op midfield — ze bereiken de enemy-base 24-36%.
  Maar `flagTaken=0`/`flagCaptured=0`: ze komen NABIJ de enemy-base maar raken de exacte vlag niet
  en voltooien geen run. Echte gat = precieze grab + carry-home (langste, meest val-gevoelige been).

**Besluit (no-thrash, aim-subsystem is delicaat — Fix A schaadde cc2 0.79→0.67):** GEEN tweede
reward-change stapelen op void_avoidance (40 min oud). void_avoidance pakt de val-vrees-helft van de
stall aan. Eerst ≥1u stabiel trainen + meten of enemy-base-reach/flagTaken stijgen. QUEUED follow-up
indien stall blijft: **fire-gate de holding-aim-bonus** (enemy_alignment telt alleen tijdens vuren →
"pointer vasthouden, niet schieten" stopt met lonen → engageren of doorlopen). Fundamenteel anders
dan de gefaalde range-gate (Fix A): behoudt lange-afstand-hitscan-aim-training (vuren op elke afstand
blijft belonen). Aparte thread: damageDealt-meting (enemies-array/instigator-slot) — silently-0
combat-rewards, waard om te fixen maar geen prioriteit boven vallen/caps.

**Baselines (06-13 ~11:25, frozen policy + void_avoidance net live, meetlat voor effect):**
objectiveProgress -40.6 · selfDamage -18.9 · voidAvoidance -0.15 (firing) · viewAlignment +497 ·
enemy-base-reach ALPHA 35.9%/BETA 24.0% · flagTaken/Captured 0 · trainer ret 2.10 step 186k.

## Monitoring (void_avoidance live ~11:25)
**Schone meetlat (gefixte fallrate.sh — `$5==$5+0` faalde op comma-locale; nu LC_ALL=C):**
- **void-fractie 23.31%** (Z<-2800), frac(Z<-2300) 28.88%, X-spread 6721 (GEEN freeze), Z p50 -1994.
  Matcht journal 20-27%-band. = THE val-meetlat; moet dalen over uren zonder X-spread-krimp.

**Cycle 1 (T+30, 11:55):** trainer gezond (step 186k→192.4k, ret 2.10 stabiel, crit 11.8→9.0,
etimes continu, 0 corrupt). Reward-trends ruisig/iets slechter (objProg -40→-122, selfDmg -18→-24)
MAAR = nog-bevroren policy (void_avoidance verandert het TRAIN-signaal, niet direct gedrag; policy
schuift pas over uren via re-export). Geen freeze/divergentie. voidAvoidance vuurt (-0.33). GEEN
interventie — te vroeg (oordeel ≥1.5u). Cycle 2 gepland T+85.

**Cycle 2 (T+85, 12:48):** Trainer SELF-RESTARTte ~12:46 (etimes=75, buf 200k→65k refill) — VRAM 98%
(bekende 4090-ceiling, [[project_sac_trainer_vram_ceiling]]). SCHOON hervat: "Loaded SAC metadata:
step=202000, Warmup already completed" (geen 946000-fallback/reset, corrupt=0). 1 restart in ~2u, geen loop.
Falls FLAT: recent-only (laatste 250k≈15min) void-frac **25.8%** vs baseline 23.3%, X-spread 6283 (geen freeze).
selfDamage -27, objProg -120, voidAvoidance -0.08..-0.33 (KLEIN vs dominante termen → scale mogelijk te zwak).
**Oordeel: inconclusief, GEEN failure** (buffer-reset 12:46 = policy her-leert void_avoidance van verse buffer;
nav = uren-schaal). Fix fallrate.sh: tail-optie voor recent gedrag (cumulatief verdunt). GEEN interventie (no-thrash).
**Primed escalatie:** als falls FLAT blijven bij duidelijke training → void_avoidance_scale 5→8 (PBRS, laag risico).
Watch: best_exported (stuck 3.09 sinds resume — policy nog niet voorbij oude best, verwacht vroeg). Cycle 3 ~T+150.

**Cycle 3 (T+150, 13:58):** Trainer continu sinds 12:46-restart (etimes 4293, step 216k, buf refilled 200k,
0 corrupt, VRAM 21.3G stabiel). **best_exported STUCK 3.09 sinds resume, ret 1.7-2.1 (<best)** = policy-return
verbetert NIET. Falls TRENDEN OP: recent void-frac 23.3(base)→25.8→**27.4%**. X-spread 5971 (licht dalend).
scale 5 deed ~niets in 2.5u.
**KEY-correctie (mijn eerdere aim-farm-framing voor de falls was FOUT):** reward is per-head gedecomponeerd
(`JointRewardDecompositionStrategy`): viewAlignment→VIEW-head; falls/void_avoidance/objectiveProgress→MOVEMENT-head.
Ze concurreren NIET → viewAlignment drowned de falls niet. Falls = puur movement-head-probleem. Binnen die head:
**objectiveProgress -103 (NEGATIEF want oprukken→val→respawn-ver)** → policy leerde "oprukken=slecht" → stalt/straft.
Falls + stall = ÉÉN munt (oprukken stopt netto-negatief zodra niet-vallen). voidAvoidance window-sum ~0 = PBRS-telescoping
(verwacht, NIET "te zwak" — per-tick aan de rand is het signaal).
**Actie: void_avoidance escalatie scale 5→8 + clamp 2.0→3.5** (primed, na 2.5u vlak). Per-tick approach ~-2.4 (was capped),
step-off cap -3.5. Trainer-veilig: rewards.json scp → 5 hosts + start-bots (jar ONGEWIJZIGD, sac_rl_pawn ONGEMOEID).
Watch: recent void-frac MOET dalen, X-spread >~4000 (freeze→scale 6), selfDamage↓. Vlak ondanks scale 8 → geodesic densify
(de echte diagonaal-pull-root) i.p.v. nog sterker. NB stall(user-observatie) lost mee op als falls dalen (zelfde munt).

## Val-MECHANISME definitief (onset-state-machine, 1965 onsets / 500k samples)
Eerdere onset-pogingen faalden (single-step >-2300→<-2800 mist de val die door de -2300..-2800-band
glijdt over meerdere samples). ECHTE state-machine (per-session floor-flag die over de transitieband
heen blijft): **1965 onsets**.
- **Step-off-snelheid |vx|=63 |vy|=60 = WALK-speed** (~66/150ms), NIET dodge-fling. Slechts 9.6%
  (188) heeft fling-snelheid (|vy|>100); **91% = geleidelijke walk-speed drift** (driftY<=50: 761, midY: 1016).
- Onsets clusteren op **midfield (X~2959)**, gespreid over de Y-walkway, ~50/50 vooruit-van-rand (xdom 1016)
  vs zijwaarts-van-rand (ydom 949), meanOnset Y -131 (gecentreerd, spreidt naar ±1500-rails).
- **CONCLUSIE:** vallen = geleidelijke walk-speed edge-drift langs de smalle midfield-walkway, GEEN
  dodge-flings → dodge-suppressie is GEEN fix (hypothese gefalsifieerd). Dit is precies wat een PBRS
  edge-gradient KAN afleren (bij walk-speed ~2.4 ticks probe-waarschuwing = genoeg reactietijd ALS de
  gradient sterk genoeg is). **Valideert void_avoidance + scale-8-escalatie.** De journal-conclusie
  "reward-shaping kan vallen niet fixen" was prematuur — nooit een echte all-directions PBRS-potentiaal
  op voldoende sterkte geprobeerd (B/D/E/F waren eenzijdig/zwak/verkeerd-frame). Vervolg bij falen scale 8:
  geodesic densify (centerline-routing) of langere floor-probe (meer reactietijd), NIET dodge/blind-scale.

**Cycle 4 (T+60 scale-8, 15:07):** Trainer continu (etimes 8459, step 230k, 0 corrupt, VRAM stabiel).
Falls FLAT: recent void-frac 26.8% (was 27.4%). X-spread 5480 (>4000, geen freeze). **ret DAALT 2.10→1.69→1.43,
best_exported stuck 3.09 (4u)** = policy verbetert niet. selfDamage -18 (licht beter). scale 8 vuurt (-1.15/window)
maar verlaagt vallen niet.

## Round 3 — PERVERSE PRIKKEL gevonden: exploration beloont VALLEN (gevalideerd + gefixt)
Reward-decompositie-analyse → hypothese: first_visit-novelty (3D-voxel, 0.2/voxel) beloont vallen want een val
passeert tientallen nooit-bezochte VOID-voxels. **DATA-VALIDATIE (laatste 300 windows, gebucket op selfDamage):
val-windows exploration 6.70 vs 4.61 zonder val** → vallen farmt ~+2 exploration/val-window. exploration zit in
DEZELFDE movement-head als void_avoidance (beide ACTION) → ze heffen elkaar deels op → verklaart waarom scale-5/8
de vallen niet brak. (selfDamage's -50 zit in de FIRE-head, niet movement → movement-head's enige netto val-deterrent
was void_avoidance, deels geneutraliseerd door de exploration-farm.)
**Fix (structureel, gevalideerd): grounded-gate op exploration** — telt alleen wanneer `collisions.floorBelow <= 600uu`
(grounded/normale sprong); satureert (~2048) zodra voeten over de void → val farmt geen novelty meer. Nieuwe param
`exploration_grounded_floor_below_max_uu=600`. Map-onafhankelijk (elke void/lava/klif-map). CollisionsDto.floorBelow
("0=grounded, max=mid-air over deep void") bestond al. Build groen, catalog-parse OK (deep-merge), GEEN nieuw signaal/
arity/routing → geen fleet-crash-risico. Deploy: sync-code (jar+rewards.json) 5 hosts + start-bots (trainer ONGEMOEID).
**Verwachting:** movement-head krijgt nu een SCHONE val-deterrent (void_avoidance −, zonder exploration-+-voor-vallen) →
scale-8 void_avoidance kan eindelijk werken. **NU PATIENCE:** ≥1.5-2u stabiel trainen zonder changes (schone reward).
Watch: recent void-frac MOET dalen; val-windows-exploration ≈ no-val (gate werkt); X-spread behouden; ret herstelt.
Bij falen na ≥2u → geodesic densify (centerline-routing) of langere floor-probe (reactietijd). NB stall=zelfde munt.

**Cycle 5 (T+90 exploration-gate+scale8, 16:50): EERSTE POSITIEVE CYCLUS.** Meerdere metrieken
samen omhoog (coherent, geen ruis): objectiveProgress **-116→-78.7** (minder terugtrekken),
recent void-frac **27.4(peak)→24.7%**, enemy-base-reach ALPHA 35.9→**40.8%** / BETA 24→**27.8%**,
trainer ret **1.43→2.00** (hersteld), crit **11→6.58** (value-fn convergeert), total 144→**225**.
X-spread 5732 (geen freeze). Trainer continu ~4u (etimes 14616, 0 corrupt). Onder de schone reward
(geen exploration-farm + scale-8) rukt de policy meer op, valt iets minder, bereikt enemy-base vaker.
Caveat: MODEST (falls ~baseline 24.7 vs 23.3, nog niet eronder); geen flag-caps; frag breakdown 0
(maar 135899 kill-announces = kills gebeuren). Gate-validatie (val-windows exploration 7.02 vs 2.46)
CONFOUNDED door fresh-process ground-exploration + traversal-correlatie (actieve bots exploreren ÉN
vallen) — niet schoon leesbaar, maar de gedragsverbeteringen zouden niet gebeuren als de void-farm
nog domineerde. **Besluit: PATIENCE — het werkt, niet aanraken (no-thrash).** Cycle 6 ~T+180. Watch:
void-frac MOET verder dalen (<baseline), enemy-base-reach↑, eerste flag-takes. Plateau bij ~24% →
dense walkable-surface geodesic-veld (filter 52k play-trace-veld op Z>-2250 = walkable-core ~3700 nodes).

## Round 4 — User-vraag "is de movement-reward correct genoeg?" → SAC-update-analyse + DEMO
**Cycle 6 (T+180): nav GEPLATEAUD.** Falls vast 24.7%, enemy-base-reach vlak (40.8/27.8%), flagTaken/Captured=0,
**movement-std vast op 0.55** (aim 0.11). Wél: crit 11→5.70 (convergeert), selfDamage -23→-9.9, damageTaken -113→-87,
shotOnTarget 17→26. Dus combat/survival verbetert, nav-flag-run muurvast.

**Diepe SAC-analyse (sac_step.py + networks.py + sac.json + inference-pad):**
1. Decompositie is OK — `compute_sac_actor_loss` (multi_head) optimaliseert de GEWOGEN SOM van alle head-Q's,
   dus de dood-straf (fire/residual-head) vormt movement WEL (indirect via Q_fire(s,a)-afhankelijkheid van moveYaw).
2. **De echte bottleneck: deterministische continue collectie.** De ONNX exporteert de MEAN; `MovementActionDecoder`/
   `RLPawnActionDecoder` doen `tanh(mean)` ZONDER ruis voor de continue dims (sin/cos heading, yaw, pitch). Alleen
   binaire dims (fire/dodge/jump/duck) worden Bernoulli-gesampled (deterministic_inference=false). → de bot probeert
   NOOIT een alternatieve heading → kan de niet-vallende flag-run niet ZELF ontdekken.
3. **Movement-std-mismatch.** target_entropy[0,1]=-0.75 → std 0.55 (vs aim std 0.09). De trainer-actor sampelt headings
   BREED (0.55) maar de critic is op mean-heading-data getraind → onbetrouwbare extrapolatie → geen convergentie
   (std blijft 0.55). Aim convergeert wél (smalle std blijft dicht bij de data). **Movement-reward niet "fout", maar
   movement-leren is structureel moeilijker (geen per-stap-exploratie + brede std bij deterministische data).**
   Lean fix-kandidaat: target_entropy[0,1] -0.75→-1.5 (commit aan precieze heading near-data).

**DEMO (user maakte er een): Attack_CTF-Face-5v5-ShockRifle2.zip** — 18890 frames (~2.8min, 5v5), recorded player
Noxi pakt+draagt de RedFlag **4947 frames** (t=38.5s→162.9s) = echte volledige flag-run. Standaard BC-format
(KeyboardCapture-acties + volle Players-state). = precies de ONTDEKKING die de bot mist.
- **Geodesic-from-demo GETEST + AFGEWEZEN:** dry-run uit demo-only → 1528 nodes, **0/100 spawn-paren verbonden** (te
  dun, gefragmenteerd). Eén sessie dekt te weinig. Demo's waarde = NIET de route-gradient maar het GEDRAG.
- **Plan = SACfD** (demo-transities in de SAC-buffer): geen model/BC-reset (policy gaat door), geen forgetting
  (capture-reward houdt het vast). Converter `DemoToExperienceMain` GEDELEGEERD aan sub-agent met harde validatie-gate
  (state-vectoren MOETEN exact matchen met GenerateTrainingCsv, anders garbage). Offline → verstoort live training niet.
- **Injectie-plan:** wanneer converter gevalideerd → demo-npz in buffer + BUNDEL de entropy-fix (target_entropy[0,1]→-1.5)
  in ÉÉN trainer-restart (demo=ontdekking, entropy=commitment). Watch: flagTaken emergentie, movement-std daalt, falls↓.

## Round 5 — STRUCTURELE FIX: looprichting-exploratie (RL leert het zelf) — user dwingt terecht weg van band-aids
User-kritiek (terecht): mijn voorstellen waren lapmiddelen; config-tweaks lossen het niet op; RL moet het ZELF leren;
+ live-regressie (bots staan stil 60% + springen). Mijn void_avoidance scale-8 veroorzaakte een idle-optimum →
TERUGGEDRAAID (scale 0). Demo/SACfD spoor verlaten (live-buffer-injectie destabiliseerde de critic 1595, + tegen de
"RL zelf"-bedoeling).
**Reward-audit (user-vraag "klopt de reward?"):** objective_progress is CORRECT (advancen wordt gecrediteerd, respawn-ver
geclampt op −6, dood = terminaal). MAAR data: STILSTAAN → objProg ≈ −1.6/window (neutraal); BEWEGEN → **−154/window**
(enorm negatief). Dus bewegen wordt zwaar bestraft want de beweging gaat de VERKEERDE kant op (weg/val). Reward klopt,
de policy stalt rationeel. Geen reward-bug, geen finetuning-kwestie.
**ROOT CAUSE (eindelijk de echte):** de bot **exploreert zijn looprichting NOOIT**. `MovementActionDecoder`: alleen
binaire dims (fire/dodge/jump/duck) Bernoulli-gesampled; de continue sin/cos = deterministische mean. + moveYaw is
8-DISCREET (`CommandController.computeMoveYaw` uit forward/back/left/right-flags; sin/cos gesnapt naar 8 sectoren + hysteresis).
Dus de bot koos elke tick deterministisch dezelfde sector → GEEN sector-variatie in de buffer → SAC-critic kan Q(s, andere-sector)
niet leren → movement-policy bevroren (log_std vast 0.55). Aim leert wél (sterk signaal trekt de mean); movement had geen
exploratie om uit te leren. = waarom RL het niet zelf oppikte.
**FIX (code, geen config-getal):** `movement_exploration_std=0.5` (sac.json) → `MovementActionDecoder` voegt Gaussiaanse
ruis toe aan pre-tanh sin/cos tijdens collectie (stochastic) + slaat de hysteresis over → de gesnapte sector wisselt →
de buffer bevat alternatieve richtingen → RL ontdekt de goede route zelf (standaard SAC-exploratie die voor de continue
dims ontbrak). Alleen looprichting; yaw/pitch deterministisch (aim werkt). Files: MovementActionDecoder (noise+skip-hyst),
RLConfig (+requireDouble +getter), RLPawnExecutorAiController (pass std), sac.json. Build groen, Python laadt sac.json schoon.
Deploy: sync-code 5 hosts + start-bots (trainer ONGEMOEID, etimes continu 26612). 0.5 ≈ policy log_std (dekt actor-sample-range).
**Watch (uren):** movement-std ontdooit? objProgress→positief? flagTaken emergentie? Kort: schokkeriger + meer vallen
(exploratie-kost, verwacht). Te chaotisch → std 0.3; te weinig effect → 0.7. Dit is de eerste echte "RL leert het zelf"-hefboom.

## Round 5b — exploratie-check 1 → idle-sampling toegevoegd + 4070 DISK-FULL gevonden+gefixt
**Check 1 (T+30 sin/cos-exploratie):** idle_rate 0.60→**0.688** (bot idlet MÉÉR), movement-std onveranderd, X-spread laag.
Diagnose: de sin/cos-exploratie geldt alleen als de bot BEWEEGT, maar de idle-keuze (dim 5) was deterministisch
(sigmoid+hysteresis, niet gesampled). De policy zat in het stall-optimum (idle) → de ruis maakte het zeldzame bewegen
zelfs slechter → de bot vluchtte naar idle → exploratie nooit toegepast.
**FIX (completeert de hefboom):** idle-keuze óók Bernoulli-sampled (op sigmoid(idle-logit), geen hysteresis) tijdens
collectie. Bot beweegt nu soms tóch (Bernoulli) → de sin/cos-ruis genereert directionele variatie → de critic leert
welke richting wél veilig oprukt. MovementActionDecoder-edit, build groen, deploy 5 hosts + bot-restart (trainer ongemoeid).
**4070 DISK-FULL (operationeel, kritiek):** sync naar 4070 faalde intermittent → schijf **100% vol** (747MB free).
Oorzaak: `rl-replay-buffer/rl_pawn` = **1.6TB / 91031 npz**, 90187 ouder dan 60min (backlog sinds 06-11) — de
experience-cleanup-na-sync op 4070 was kapot (andere hosts hadden 6-102 npz = schoon). Disk-vol = 4070 (5 instances,
de meeste bots) kon geen experience meer schrijven → trainer kreeg minder data = verborgen rem op het leren.
Cleanup (>60min npz, niet-ingestbaar want max_file_age 1u) → **20% used, 1.4TB free**. Safety-cron gezet (elke 20min).
4070-jar nu 20:39 (idle-sampling erop). Trainer continu (etimes 28734, 8u).
**Watch:** X-spread MOET stijgen (bot beweegt meer door idle-sampling), dan sector-exploratie → leren (objProgress↑,
movement-std ontdooit, flagTaken). Trainer krijgt nu ook 4070's experience terug.

## Round 6 — 4070 was GECRASHT (niet gestart) + sync root-cause + idle-sampling-effect
**User-melding:** "de 4070 is nog niet gestart?" → klopte. 4070-bots crashten **om 20:41 bij startup**:
de schijf stond op dat moment nog 100% vol → `GenericPredictor.<clinit>` kon de onnxruntime native-lib niet
uitpakken (`No space left` → `UnsatisfiedLinkError: no onnxruntime`) → JVM dood, 0 npz sinds 20:41 (~20min donker).
**Fix:** `start-bots.sh --host desktop-4070` → 5 ucc-servers + MultiInstanceLauncher vers op, 0 her-crash,
**220 npz/2min** = experience stroomt weer. Disk stabiel 19%.

**Sync root-cause (structureel, niet de eerder vermoede filter-bug):**
- Route + filter zijn CORRECT (`rl_pawn -> 4090`, `batch_4070_*`, MACHINE_ID=4070). Sync_replay draait.
- **De npz zijn ~22MB per stuk** (de comment in sync_replay nam 12KB aan — 1800× mis). 5 instances ⇒ ~44MB/s
  productie, maar `--bwlimit=20000` capt de uplink op 20MB/s ⇒ ~24MB/s surplus stapelt op tot disk-vol.
  Dát is de oorzaak van de 90k/1.6TB-backlog, niet een naam-mismatch.
- Trainer is NIET uitgehongerd: 200k-ring vol, ingest 4600-7200 transitions/3s, 4070 bereikt hem (231 files in
  trainer-queue). De surplus die 4070 méér produceert dan de trainer kan gebruiken is *echt* surplus.
- **Structurele fix (i.p.v. mijn host-specifieke cron):** retentie-sweep in `sync_replay.sh` NORMAL-loop —
  `find ... batch_${MACHINE_ID}_*.npz -mmin +60 -delete`. Trainer negeert >max_file_age(3600s) toch, dus dit
  dropt enkel niet-ingestbare surplus; beschermt élke high-productie host, in-git, naar alle 5 hosts gedeployed.
  Live bij volgende bot-restart; cron dekt de tussentijd.

**Idle-sampling-effect (T+~45min, het echte leersignaal):**
- idle_rate **0.688 → 0.397** (bot staat veel minder stil), X-spread **3966 → 5949 (+50%)** = traverseert nu de
  centrale brug i.p.v. stilstaan. ✓ mechanisch werkt de hefboom.
- Falls OMHOOG (Z<-2800: 17%→25%; Z<-2300: 21%→31%), objProgress **-60 → -129**, selfDamage -13→-25.
  = verwachte exploratiekost: hij waagt zich nu écht op de brug over de void en valt er nog af. Dít is wat de
  critic moet leren ("blijf op de brug"). idle regresseert (nog) NIET ⇒ policy zegt nog niet "bewegen=slecht".
- crit 7.5→12.5 (critic fit de nieuwe diverse data), corrupt=0, flagTaken=0.
**Beslissing:** std=0.5 VASTHOUDEN (geen thrash), uren laten leren. Abort-signaal = idle_rate terug >0.6 of
objProgress blijft dalen + X-spread zakt. Watch: objProgress-trough → herstel, falls → omlaag, eerste flagTaken.

## Round 7 — TREND POSITIEF: critic leert de oversteek (objProgress herstelt uit dal)
Reeks sinds idle-sampling deploy (~21:01):
| T+min | step | crit | idle | falls(Z<-2800) | X-spread | objProgress | selfDmg |
|---|---|---|---|---|---|---|---|
| 45  | 303700 | 12.5 | 0.40 | 25.4% | 5949 | -129 (dal) | -25 |
| 93  | 313700 | 11.6 | 0.49 | 28.8% | 5939 | -121 | -32 |
| 120 | 319600 | 12.3 | 0.34 | 29.1% (piek) | 5536 | -114.8 | -27.7 |
| 147 | 324900 | 10.4 | 0.43 | 27.4% ↓ | 5475 | -87.4 | -23.4 |

**Lezing:** exploratiekost piekte rond T+45-93 en HERSTELT nu. objProgress monotoon -129→-87 (+42/100min ≈
+0.41/min), falls gepiekt+dalend (29→27%), selfDmg -32→-23. idle stabiel 0.34-0.49 (GEEN regressie naar >0.6).
crit convergeert 12.5→10.4, corrupt=0, trainer +21k steps. X-spread zakt licht (5949→5475) maar blijft >> pre-fix
3966 — bot wordt iets behoudender terwijl falls dalen (consistent). flagTaken nog 0.
**Conclusie:** de structurele exploratie-fix WERKT — de critic leert uit de rijkere midfield-data welke richting
veilig oprukt, precies het mechanisme dat user's "reageert-midfield-maar-vergeet-na-respawn"-zorg adresseert.
Projectie: objProgress→0 in ~3-4u bij dit tempo; flag-takes kunnen eerder emergeren als één bot betrouwbaar de
enemy base haalt. **Geen config-change (no-thrash tegen werkende trajectorie).** Overnacht 70min-cycli, deep check
+ journal per cyclus. Abort-signalen ongewijzigd (idle>0.6 / X-spread<4000 / objProgress 2+ checks niet herstelt).

## Round 8 — plateau-diagnose: Option B afgevoerd, std 0.5→0.3 (signaal-ruis) + structurele retentie live
**Bevestiging:** geen regressie maar PLATEAU. idle oscilleert (0.631→0.410→0.547, laatste <0.6 = ruis-piek,
abort-regel niet echt af), objProgress bounced -133→-105. Maar 3u VLAK: objProgress range-bound ~-110, falls ~28%,
log_std bevroren 0.55, X-spread stabiel ~5500, 0 flag.
**Option B (sample uit policy-log_std) AFGEVOERD:** `log_std_param` is een GLOBALE [action_dim]-parameter
(networks.py:187), niet state-conditioneel, en hier bevroren op 0.55 ≈ mijn vaste 0.5 → B = no-op, geen annealing-winst.
**Diagnose plateau:** vlakke critic over heading. 0.5 pre-tanh-ruis op de smalle brug VERDRINKT het vooruit=progressie-
signaal → critic ontwikkelt geen heading-voorkeur → policy-MEAN sharpent nooit richting brug → falls dalen nooit →
'bewegen=riskant'-bekken → idle kruipt. Met MINDER ruis: schonere credit → mean wijst betrouwbaarder over de brug →
falls dalen via MEAN-verbetering (niet via ruis-annealing — mijn eerdere 'falls dalen nooit'-zorg gold alleen als de
mean stilstaat).
**ACTIE:** movement_exploration_std 0.5→0.3 (≈2.7× target-std 0.11, nog ruim exploratief). sac.json gesynct naar 5
hosts, bots herstart op 4 bot-hosts, TRAINER ONGEMOEID (Java-side param, geen checkpoint-risico). Bots gezond op
(etimes ~45s, 0 crash), trainer etimes 41800/step 341400 continu. **Bonus:** de structurele retentie-sweep in
sync_replay.sh is nu LIVE (cron redundant).
**Beslisboom volgende cyclus:** objProgress↑ + falls↓ ⇒ signaal-ruis was de rem (evt 0.25). Revert naar frozen
(objProgress~-60 / idle~0.69 / X-spread~4000) ⇒ 0.3 te laag, exploratie-STRUCTUUR is de rem → prioritized-replay op
rare oversteken (user's 'raakt-midfield-maar-vergeet'). Plateau blijft ⇒ magnitude is niet de hefboom → structureel.

## Round 9 — failure-mode genageld: hard-exploration DEADLOCK (niet aim-farm — die is al verworpen)
**std=0.3 effect (vs 0.5-plateau):** lichte verbetering — objProgress ~-110→~-90, falls 28→25%, NIET teruggevallen
naar frozen (X-spread 5600+). Signaal-ruis was *een* factor maar geen doorbraak; nog 0 flag.
**Ruwe reward-distributie (300 windows, 4070):** viewAlignment mean ~440 (dens, near-max 570), objProgress mean -99
maar **max +91** (oversteken gebeuren, zeldzaam), shotOnTarget ~17 (100% windows) maar **frag ~0** (landt treffers,
doodt nooit), flagTaken 0. = **midfield aim/combat-IMPASSE**.
**viewAlignment-farm AL GETEST+VERWORPEN (06-11):** de `enemy_alignment_max_range_uu` gate bestaat al maar staat op
100000 (=UIT). Doc: gate@2500 (a) brak de cowardice NIET (bot rukt niet op; root-cause = combat-onvermogen op afstand)
en (b) degradeerde hitscan-aim (cc2_fire_aim_alignment 0.786→0.670, want shock-primary lange-afstand-aim is legitiem).
⇒ aim-farm is NIET de hefboom; dat spoor is dood. (Goed dat ik checkte vóór herhaling.)
**Echte diagnose = hard-exploration DEADLOCK:** oversteken zijn zeldzaam (max objProgress +91) → de critic leert
Q(oprukken) nooit positief (oprukken = vallen 25% OF doodgeschoten, frag 0) → policy rukt terecht niet op → nog minder
successen. User's observatie sluit: home→midfield vallen ze (navigatie); vóórbij midfield "blijven ze wel op de map"
maar stoppen+aimen+strafen i.p.v. doorlopen (lokaal optimum, niet de val).
**VOLGENDE STRUCTURELE LEVER (= user's 'raakt-midfield-maar-vergeet'-hypothese): prioritized replay op zeldzame
hoge-objProgress oversteken.** Mechanisme (`_compute_priority_masks`) ondersteunt het, maar de min_abs_reward-drempel
is GLOBAAL → schone selectie van crossings vraagt een PER-KEY drempel (kernel-change, replay_buffer.py + config.py +
sac.json). Te valideren met test_replay_buffer_decomp.py vóór deploy; trainer-restart = checkpoint-risico (survivable:
revert naar champion, GEEN BC-reset). **Deploy als SCHOON volgend experiment ná std=0.3-settle (geen confounding).**

## Round 9b — correctie volgende lever: gamma 0.99 vóór prioritized-crossings (lager risico)
Bij het lezen van `sample_event_prioritized` + `_compute_priority_masks`: prioritized-crossings vereist een PER-KEY
drempel op de dense `movement`-decomp (de globale min_abs_reward 1e-6 zou bij toevoegen van 'movement' de event_mask
SATUREREN → fire/altFire-prioriteit kapot). Die per-key drempel goed zetten vraagt de per-transitie movement-reward-
distributie + waarschijnlijk meerdere trainer-restarts om te tunen = veel kernel-churn + checkpoint-risico voor een
onzekere drempel.
**Lager-risico, vooraf-gesanctioneerde lever die DEZELFDE deadlock aanpakt: gamma 0.97 → 0.99.** De deadlock = Q(oprukken)
blijft laag omdat de verre vlag-payoff zwaar gediscount is op het midfield-beslismoment. Langere horizon (~3s→~8s)
propageert die payoff verder terug = exact 'raakt-midfield-maar-vergeet'. Eén config-regel (sac.json-doc noemt 0.99
expliciet als geplande volgende stap, "Reversibel"), GEEN kernel-change, critic her-convergeert vanzelf (gamma is geen
netwerk-param → checkpoint/actor blijven geldig, geen buffer-clear nodig). reward_normalization aan tempert de extra
variance. **Plan:** na std=0.3-settle-read — als plateau bevestigd op ~-90 (niet meer stijgend): gamma→0.99 + SCHONE
trainer-restart (verifieer checkpoint-mtime vóór/na, geen mid-write). prioritized-crossings (kernel) = pas erna indien
gamma niet volstaat.

## Round 10 — bots draaien WÉL live policy (stale-zorg weerlegd) + gate-placeholder + critic-volatiliteit
**Bots draaien live nacht-policy ✓:** ModelWatcher laadt `trainingmodel/rl_pawn.onnx`, mtime op 4070 = 03:23 (vers,
+.onnx.data). Export-naar-bots staat LOS van de gate. (Belangrijk inzicht: mijn idle-sampling/std-changes zijn JAVA-side,
dus gedrag verandert ongeacht het ONNX — gedragsbewijs alleen bewijst niet welk model draait. mtime + ModelWatcher-pad
wel.) Plateau is dus de échte getrainde policy.
**DualKPIDeltaGate = [PLACEHOLDER] hele nacht:** elke cyclus combat_score=0.000/movement_flag=0.000, "baseline.json
ongevuld, wacht op eerste valide meting voor auto-init". → geen champion-promotie (champions/ laatst 06-10) + GEEN
rollback-safety. sac_best.onnx + best_exported=3.091 vast sinds 06-10 (gate blokkeert best-export). NIET aankomen
([[feedback_delta_gate_untouched]] + auto-init is verwacht na from-scratch). Maar besef: een regressie bereikt de bots
ongefilterd.
**Observability is ER (geen gat):** policy heeft self_floorBelow_norm + self_fwd/back/left/right(+diag)FloorDelta_norm
(8-weg floor-drop) + self_physics_isFalling. Bot KAN de afgrond waarnemen → aanhoudende falls zijn deels exploratie-ruis-
artefact (ongerichte heading-ruis duwt van de rand óók als de edge-bewuste policy het zou mijden). Mijn falls-metriek is
collectie-met-ruis = opgeblazen, niet de echte policy.
**Critic-volatiliteit (watch):** crit ~10 → oscillerend 18-31 sinds step 377300 (~03:08); ret STABIEL ~1.5 (geen
policy-collapse), corrupt=0. Critic-loss-ruis, geen actor-collapse. Escalatie-drempel: ret<1.0 of crit>50 sustained.
**Nacht-besluit:** GEEN trainer-restart nu (vanuit volatiele critic + geen rollback + user slaapt = te riskant; kans de
377k-step-training te verliezen). Trainer laten doorlopen, stabiliteit scherp bewaken. prioritized-replay-crossings
(per-key drempel, kernel-change) blijft DE volgende principiële lever (user's hypothese), maar bewust deployen bij
stabiele critic + checkpoint-backup vooraf — niet blind om 03:30.

## Round 11 — scherpste diagnose: ZERO-SUCCESS exploration (geen oversteek om te versterken)
**reward_movement-distributie (NPZ, n=512):** mean -0.36, std 2.46, range ±6, roughly symmetrisch (advance + / retreat-val −).
Positieve staart: frac>2=11%, frac>3=1.8%. Dus sterke-vooruit-frames bestaan — MAAR met flagTaken=0 zijn dat *partiële*
advances die in val/dood eindigen, GEEN voltooide oversteken.
**Kern:** zero-success exploration. Bot haalt midfield (X-spread ~halve map) maar nooit de enemy-base → RL heeft geen
succes om te versterken. Buffer churnt 2-3min → een toevallige oversteek wordt "vergeten" vóór duurzaam geleerd (= user's
observatie, mechanistisch bevestigd).
**Implicatie voor levers:**
- Config-tweaks cracken dit niet (std/aim-farm/observability/gamma uitgesloten of onwaarschijnlijk).
- prioritized-replay: geen VOLLEDIGE successen om te prioriteren; zou partiële advances (movement>2) up-samplen om
  "vooruit=lonend" langer levend te houden → kan oprukken-bias geven, ONZEKER. Vereist kernel-uitzondering (sac_core =
  stabiele kernel, alleen voor algorithm-bugs) → user-beslissing.
- Organische lever die overblijft = TRAININGSTIJD (exploratie voltooit ééns een oversteek → bootstrap). Hard
  zero-success probleem; geen schone autonome overnight-fix.
**Besluit:** gezonde trainer (crit hersteld ~10, ret ~1.5, corrupt=0) langer laten doorlopen + scherp bewaken; geen
risicovolle/regel-schendende changes blind om 05:00. Morgenvroeg: eerlijk beeld + prioritized-replay-voorstel (met
zero-success-caveat + kernel-uitzondering) voor user's keuze.

## Round 12 — EDGE-AWARE EXPLORATIE geïmplementeerd + gedeployed (Java-side, generiek)
**Waarom:** vallen = DOMINANTE doodsoorzaak (~27% Z<-2800, val-suicides >> combat-frags 0), grotendeels
exploratie-ruis-artefact (uniforme heading-ruis duwt de bot van de smalle CTF-Face-brug óók al heeft de policy
edge-features). Meer-tijd cracked het niet (6u plateau, 0 flag). Config-tweaks/aim-farm/gamma/observability uitgesloten.
**Wat:** edge-aware demping van de heading-exploratie-ruis. `edgeDropAmount = max over 8 floor-richtingen van
-tanh(floorDelta/64)` ∈[0,1] (0=vlak, 1=void; doc CollisionsDto r82 = teken-correct). severity ramp 0@drop_threshold(0.6,
~44uu) → 1@void; effectieve std = std×(1 − severity×(1−edge_scale)), edge_scale 0.3. Op vlakke grond = volle exploratie;
op de brug = edge-bewuste policy-mean stuurt, ruis ~30%. FRAME-AGNOSTISCH (min over alle richtingen) → geen
perspectief-normalisatie-bug. 4 files: sac.json (2 params + doc), RLConfig (read+getters), MovementActionDecoder
(4-arg ctor + decode-overload met edgeDropAmount, schaalt ENKEL de ruis-magnitude — idle-sampling/hysteresis ongemoeid),
RLPawnExecutorAiController (berekent edgeDropAmount uit currentFrame.playerPawn.collisions). Backward-compat (2-arg
ctor → edge_scale 1.0 = uit; oude decode → drop 0). Model leert nav nog steeds zelf; dempt enkel zelf-sabotage.
**Deploy:** build groen, jar+sac.json → 4 bot-hosts, bots herstart, TRAINER ONGEMOEID (Python negeert de keys, geen
restart, geen checkpoint-risico). Health ok (0 config-crash = RLConfig las de params, 232 npz/2min, trainer etimes
65895/step 424000). Reversibel via edge_scale=1.0.
**Watch:** falls(Z<-2800) MOET dalen vanaf ~27%; X-spread MOET hoog blijven (collapse = bot bevroren door over-gating →
edge_scale↑ of threshold↓); objProgress↑ (minder retreat/val); idle stabiel; flagTaken; trainer crit/ret stabiel.
Tune: falls dalen weinig → edge_scale↓ (0.2/0.15) of threshold↓ (0.5); X-spread collapse → edge_scale↑ (0.5).

## Round 13 — edge-aware NUL effect → falls zijn POLICY/REWARD-gedreven (niet ruis); root-cause = credit-balans
**edge-aware resultaat (+52min):** falls 27.5% EXACT onveranderd, X-spread 5680 (stabiel), objProgress ~-105, flag 0,
trainer gezond (crit ~12, ret 1.7, corrupt=0). → de gate vuurt niet proactief OF (sterker) de falls zijn niet
ruis-geïnduceerd. Combineer met std 0.5→0.3 (óók ~0 effect op falls): **de falls zijn POLICY/REWARD-gedreven** — de
bot KIEST acties die van de brug leiden, immuun voor exploratie-tweaks. (edge-aware was diagnostisch waardevol: bewees
dit; code blijft, edge_scale 0.3 = harmloos/onbewezen, reversibel.)
**Root-cause hypothese (= user's reward-vraag): credit-balans.** GeodesicField.distanceOrNaN = min over nabije nodes
||from−node|| + graafafstand(node→doel); off-bridge=NaN→objProgress 0 (val zelf niet bestraft). Maar dense
approach-progressie (progress_scale 0.12 × max-delta 50 = +6/frame) >> sparse val-dood (residual −1.5): één approach-frame
overtreft de hele val-straf → "ruk agressief op, vallen = acceptabele kost" is netto-positief → policy leert het.
**Niet blind in de reward thrashen (user-domein, uitvoerig getuned).** Voorgelegd aan user met 2 structurele opties:
(1) terminal void-penalty gekalibreerd op de geaccumuleerde approach-winst (NIET de scale-8 band-aid die idle brak);
(2) progressie-credit gaten wanneer niet-grounded/over-drop (alleen progressie op begaanbare route). Wacht op afstemming.
**Trainer:** 18,5u continu, gezond, blijft doorleren op de huidige setup.

## Round 14 — OPTIE A: terminale void-fall penalty (user: "doe maar A") — GEDEPLOYED
**Root-cause (Round 13, gevalideerd):** dense approach-progressie >> sparse val-dood (−1.5) → "oprukken-dan-vallen"
netto-positief → policy KIEST de val (falls immuun voor std/edge-exploratie-tweaks).
**A:** terminale val-dood-penalty, ONAFHANKELIJK van de (onbetrouwbare) self-inflicted-vlag. Detectie: een instakill-val
rapporteert lastDamageAmount >> elke combat/splash-schade (≤~150 HP) → `lastDamageAmount > void_fall_hp_threshold(500)`.
Straf `void_fall_penalty(-15)` opgeteld bij selfDamage (eenmalig op de death-frame = GEEN per-frame farm zoals de
gereverteerde void_avoidance → geen idle-regressie). Via SELF_DAMAGE → fire-head (1.75); multi-head actor stuurt ook de
movement-dims weg van val-leidende acties via de gedeelde critic. Map-onafhankelijk (elke instakill-void/lava).
**Files:** DamageDeltaParams (+2 record-velden), DamageDeltaModule (read), DamageDeltaReward (void-branch na selfDamage),
rewards.json (default damage_delta weights; andere groepen erven, weapon_profiles/shock = dormante partial). Build groen,
geen andere constructors. Deploy jar+rewards.json → 4 bot-hosts, restart, GEEN parse-crash (0 op alle hosts), npz stroomt
(175/min), TRAINER ONGEMOEID (reward = bot-side, step 459500). Reversibel (void_fall_penalty=0).
**Watch (leerloop ~30-60min):** falls(Z<-2800) MOET dalen vanaf ~27%; selfDamage MOET negatiever (penalty vuurt op
fall-events); X-spread MOET hoog blijven (collapse = te bang voor de brug → penalty↓ naar -8); objProgress↑; flagTaken.
Kalibratie: falls dalen niet → penalty↑ (-30/-50); X-spread collapse → penalty↓.

## Round 15 — FLEET-FREEZE incident (user-gemeld) + resolutie
**User:** alle bots stil (behalve af en toe springen), ook de ALPHA-champions. Kwam met de void_fall=-50 restart.
**Diagnose (NIET reward/policy):** VALIDATE idle_rate=0.16 (deterministische policy WIL bewegen, geen collapse);
ALPHA-champions laden een APART model (champions/rl_pawn/0106 via ChampionNewestWatcher, bevestigd) — tóch frozen →
executie-freeze, niet het model. 4070 live: X-spread(N=2500)=0 (echt bevroren), maar inference draait (npz 49/min,
RL_FLUSH, dense reward 438-796 = bots aimen+vuren rawFire=0.896, action=-8 stuck-penalty). 2070 = appRequestExit.
Fleet-breed. = transiënte startup/executie-race ([[project_modelkey_registration_race]]-familie: "quick fix = herstart").
**Fix:** void_fall −50→−15 (matiging als voorzorg, isoleert de agressieve variabele) + schone herstart ALLE hosts.
→ 4070 X-spread 0→3130 (beweegt weer), alle hosts produceren weer, trainer ongemoeid. Freeze was dus NIET de reward.
**Les:** een restart kan transiënt freezen; verifieer beweging via RECENTE X-spread (kleine N), niet de gelagde
grote-window-X-spread (die toonde misleidend 5061 terwijl recent al 0 was). npz-productie is GEEN freeze-signaal
(frozen bots flushen door). void_fall blijft -15 (threshold 175 → val-totaal -65, uniform op alle vallen).

## Round 16 — ROOT-CAUSE combat-ineffectiviteit (agent) + aim-cone fix + void_fall REVERT
**User-observaties (grondwaarheid):** bots lopen naar de enemy i.p.v. de vlag, hangen rond de midfield-enemy zonder
effectief te schieten, vallen op midfield juist NIET, en de champions (apart model) lijken op current.
**Geverifieerd:** (1) navTarget is CORRECT (resolvt naar enemy-vlag, geen rogue enemy-pull) — geen nav-bug; (2) champion
0106 is APART (md5 verschilt) maar BEVROREN op 06-10 (gate placeholder, geen promote in 4 dagen) → lijkt op current
omdat beide hetzelfde vast zitten; (3) val-penalty A zat op het verkeerde probleem (bot valt niet op midfield).
**SMOKING GUN (agent, hoge confidence): combat farmt een veel te ruime aim-cone i.p.v. te raken.**
- `shot_min_aim_score=0.6` = 53° on-target-cone → binnen 53° = "op doel" = gegarandeerde miss op afstand, wél reward.
- Bot vuurt ALLEEN binnen die cone (shotOffTarget=0) → farmt shotOnTarget (~33/window) terwijl damageDealt ~0-2.5.
- PITCH systematisch fout: pitchAlignment negatief in 99,96% (mikt te laag); de dominante dense aim-reward
  viewAlignment (+500/frame) is YAW-ONLY → geen gradient om pitch te fixen. → vuurt constant, yaw-goed/pitch-fout,
  raakt nooit → damageDealt 0 / frag 0 → eeuwige nutteloze midfield-vuurwisseling → geen oversteek = "herhaalt fout".
- Uitgesloten: attributie (2863 windows HEBBEN damage = plumbing werkt, hits zijn gewoon 0,5% zeldzaam), altFire
  (primary 0.48 > alt 0.42, hitscan), weapon-override (primary is stock).
**FIX:** shot_min_aim_score 0.6→0.85 (cone 53°→32°) zodat de reward pas uitbetaalt bij échte 3D-aim incl. pitch →
gradient duwt aim OP de enemy. + void_fall_penalty→0 (A teruggedraaid, verkeerd probleem). Deploy schoon, geen
parse-crash, X-spread 3018 (beweegt), trainer ongemoeid.
**Watch (leerloop ~30-60min):** damageDealt MOET stijgen van ~0; frag↑; shotOnTarget mag eerst dalen (strakkere cone)
dan herstellen; X-spread blijft >0 (geen re-freeze). Vervolg indien damageDealt vlak blijft: viewAlignment pitch-aware
maken (agent #3 — de yaw-only dense reward laat pitch ongemoeid) en/of cone verder aanscherpen (0.92).
